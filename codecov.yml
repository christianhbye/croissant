# Coverage statuses are informational only: they report on the PR but
# never fail a check (large diffs were failing the default statuses).
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
